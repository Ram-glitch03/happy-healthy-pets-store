# Netlify configuration for static HTML site
[build]
  publish = "."
  command = ""

# Explicitly serve HTML files without any SPA fallback
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"

